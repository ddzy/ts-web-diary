import styled from "styled-components";


export const NavWrapper = styled.div``;
export const NavMain = styled.div`

`;

// 左边提示区域
export const NavMainTipBox = styled.div``;
export const NavMainTipText = styled.span`
  font-size: 16px;
  font-weight: bold;
`;

// 右边链接区域
export const NavMainLinkBox = styled.div`
  a {
    &:link, &:visited {
      color: #000;
    }
    &:hover {
      color: #1DA57A;
    }
  }
`;